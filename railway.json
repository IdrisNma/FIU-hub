{
  "build": {
    "command": "pip install -r backend/requirements.txt"
  },
  "start": {
    "command": "python backend/manage.py migrate && python backend/manage.py collectstatic --noinput && python backend/manage.py runserver 0.0.0.0:$PORT"
  }
}
